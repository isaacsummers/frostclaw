{
  "name": "@local/snowflake-cortex",
  "version": "1.0.0",
  "main": "dist/index.js",
  "devDependencies": {
    "bun-types": "^1.3.14",
    "typescript": "^6.0.2"
  },
  "openclaw": {
    "extensions": [
      "./dist/index.js"
    ],
    "providers": [
      "snowflake-cortex"
    ]
  },
  "scripts": {
    "lint": "node -e \"JSON.parse(require('fs').readFileSync('openclaw.plugin.json','utf8'))\" && echo 'openclaw.plugin.json: valid'",
    "build": "bun run lint && bun build ./index.ts --outfile ./dist/index.js --bundle --external 'openclaw'",
    "test": "bun test tests/index.test.ts tests/proxy.test.ts tests/beta-headers.test.ts tests/catalog.test.ts",
    "test:live": "bun test tests/live/"
  },
  "type": "module",
  "dependencies": {}
}
